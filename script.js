const header = document.querySelector('.header') //chamando do html
const icon = document.querySelector('.icon') //chamando do html

icon.addEventListener('click', () => { //quando clicar no icone
    header.classList.toggle('dark') //faça isso... css
}
)